import { AutocompleteNavigationDirective } from './autocomplete-navigation.directive';

describe('AutocompleteNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
